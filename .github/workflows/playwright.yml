name: Playwright Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Playwright dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Checkout RWA
        uses: actions/checkout@v4
        with:
          repository: cypress-io/cypress-realworld-app
          ref: 39fc97dfcb5e35f83e0fa05319fc0e2e117b9af9
          path: cypress-realworld-app

      - name: Install RWA dependencies
        working-directory: cypress-realworld-app
        run: yarn install --frozen-lockfile

      - name: Run Playwright tests
        run: npx playwright test --project=login --project=visual
        env:
          CI: true
          BASE_URL: http://localhost:3000
          USER_NAME: Heath93
          USER_PASSWORD: s3cret
          APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}

      - name: Upload test report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
